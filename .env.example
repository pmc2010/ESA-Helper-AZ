# ESA Helper Environment Configuration
# Copy this file to .env and modify as needed
# The .env file is gitignored for security and should not be committed

# Server Configuration
# PORT - The port number the Flask app will run on (default: 5000)
PORT=5000

# HOST - The host address to bind to (default: 127.0.0.1 for localhost only)
# Use 0.0.0.0 to listen on all network interfaces
HOST=127.0.0.1

# Debug & Development Settings
# FLASK_ENV - Set to "development" to enable Flask debug mode
# DEBUG - Set to "1" to enable debug mode (alternative to FLASK_ENV)
# WARNING: Never enable debug mode in production - it allows arbitrary code execution
# FLASK_ENV=development
# DEBUG=0
